$(document).ready(function(){
    // Handles click event on DIV#update_header
    $('#update_header').click(function(){
        // It updates the text of the <header> element
        $('header').text('New Header!!!');
    });
});
